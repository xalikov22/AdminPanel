// import { take_Teacher } from "./teacherTypes";

// const initialState = {
//   numOffTeachers: 10,
// };

// const reducer = (state = initialState, action) => {
//   switch (action.type) {
//     case take_Teacher:
//       return {
//         ...state,
//         numOffTeachers: state.numOffTeachers > 0 ? state.numOffTeachers - 1 : 0,
//       };
//     default:
//       return state;
//   }
// };

// export default reducer;
